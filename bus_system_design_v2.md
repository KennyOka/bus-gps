# バス運行支援システム 設計書 v2(早発防止版)

作成日: 2026-07-26
前提: [bus_system_handoff.md](bus_system_handoff.md) の検討を、目的の再定義に合わせて更新したもの。

---

## 1. このアプリの目的(再定義)

路線バスの運転手が **ダイヤ表で指定された各バス停の発車定刻を守る** ことを支援する。

> **絶対ルール:定刻より早くバス停を発車する「早発(そうはつ)」を絶対にさせない。**
> 遅れis許容される。したがってアプリの最重要機能は「早発の防止」であり、遅延の可視化は副次的。

本質は「遅延メーター」ではなく、**発車していいか/まだダメかを一目で伝える信号機**である。

- 定刻**前**にバス停に居る → 🟥 全画面・赤点滅「早発注意 — 発車しないで」+ 定刻までの残り秒カウントダウン
- 定刻**ちょうど/過ぎ** → 🟩 緑「正常運行(発車可)」

---

## 2. 確定した仕様(2026-07-26 ユーザー確認済み)

1. 実走で所要時間を測る機能は**不要**(旧プロトタイプの `scheduledMin` 測定は廃止)。定刻はダイヤ表が正。
2. 定刻 = 各バス停の**発車時刻を1つ**。到着/発車は分けない。
3. 定刻は**絶対時刻**。相対分・運行開始時の始発時刻選択は廃止。1日のダイヤは確定済み。
4. 1つのダイヤは同じ路線を複数便繰り返す(このダイヤ表では 52801便が7回運行)。
5. 早発アラート条件 = **バス停 R m 以内 かつ 現在時刻 < その停の定刻**。R 初期値 30m(可変)。
6. バス停の**位置(lat/lng)は当面、現地訪問で取得**(記録ツール)。将来はデータ入手の可能性あり。

---

## 3. データモデル

### 3-1. 「バス停」は片側ポール単位で扱う

バス停は道路の左右にあるため、**同じ名前でも往復で別の物理ポール=別の位置=別の定刻**になる。
例: 52801便の `有屋入口` は seq5(往路側)と seq8(復路側/反対車線)で2回通るが、これは別ポール。
朝の 4204/4201 も往復関係で、`浜里町`・`朝仁` 等が両方向に存在する。

→ 位置の単位は「停留所ポール(片側)」。各ポールが独立した `stop_id` と lat/lng を持つ。表示名は重複してよい。

### 3-2. ダイヤと位置は「通過順(seq)」で結合する ★重要

名前で結合すると 有屋入口 の①②を区別できない。**便内の順番(seq)をキーに結合**する。

- ダイヤ明細: `(trip_id, seq) -> stop_name, depart_time`
- バス停マスタ: `(route, seq) -> lat, lng`
- 記録ツールの**順走記録モードは走った順=ダイヤのseq順で位置を取る**ので、seqで1対1に対応する。

52801便の7便は同じ停留所列を共有するので、位置は route=52801 の19ポールを**順走1回**取れば全7便で使い回せる。
(全体では 4204=12, 4201=12, 52801=19 ポールを取得。)

### 3-3. スプレッドシート `bus_diagram.xlsx`(生成済み)

3シート構成:

- **ダイヤ明細**(縦持ち, 157行): `trip_id, digital_no, trip_label, seq, stop_name, depart_time, is_terminal, note`
- **バス停マスタ**(43行, 位置は空欄=現地入力): `stop_id, route, seq, stop_name, side, lat, lng, note`。黄色セル(side/lat/lng)を埋める。
- **便一覧**(9便): trip_id ごとの始発/終点/停留所数。

便: `4204`, `4201`, `52801_1`〜`52801_7`。

---

## 4. コア・アルゴリズム(モニター側)

現在監視中の便を1つ選び、その停留所列 `stops[1..n]`(各 `depart_time` 絶対時刻, `lat/lng`)を持つ。
`currentSeq` = 次に発車すべき停留所の seq。

```
対象停 = stops[currentSeq]
残り秒 = 対象停.depart_time - 現在時刻      // 正=定刻前(早発危険), 0以下=定刻到達(安全)
距離   = GPSと 対象停.位置 の距離(ハーバサイン)
```

**表示状態:**

| 状況 | 条件 | 表示 |
|---|---|---|
| 🟥 早発注意 | 距離 ≤ R かつ 残り秒 > 0 | 全画面赤点滅 + 「あと mm:ss」カウントダウン + バイブ |
| 🟩 正常運行 | 距離 ≤ R かつ 残り秒 ≤ 0 | 緑「発車可」+ 遅れ +mm:ss を小さく |
| 🟡 先行注意(任意) | 距離 > R かつ 残り秒 > 0 | 走行中に定刻より先行。予告表示(早着防止) |
| ⚪ 走行中 | 距離 > R かつ 残り秒 ≤ 0 | 「正常運行」中立表示 |

**seq の進め方:** 対象停に距離 ≤ R で「到着」判定 → 距離が再び R を超えて離れたら発車とみなし `currentSeq++`。
これにより、有屋入口①②のようにGPS上近接するポールも、間の停留所と時間経過で自然に区別される。

**役割分担:** カウントダウンの数字は端末時計と定刻の差(GPS不要)。GPSは「どの停が対象か/着いたか」の判定に使う。

---

## 5. 画面(運転中の一瞥性優先)

```
┌───────────────────────────────┐
│ 山羊島→佐大熊団地  14:57発便        14:57:47 │
├───────────────────────────────┤
│           次: 有屋入口                        │
│           定刻 15:06                          │
│                                               │
│            あと  1:12                         │  ← 残り秒 大きく
│      🟥 早発注意 発車しないで                   │  ← 緑/黄/赤の状態バナー
│                                               │
│  ●━━●━━◉────○────○  進行レール(seq)          │
└───────────────────────────────┘
```
赤は CSS `@keyframes` で点滅、`navigator.vibrate` 併用。緑「正常運行」は静かな表示。

---

## 6. 既存プロトタイプの扱い

- **bus_tracker.html**: 骨格(座標投影/レール/GPS/デモ)を再利用しつつ、中身を「分の遅延メーター」から
  「便選択 → seqベースの早発監視(秒カウントダウン + 赤/緑)」へ**作り替え**。ハードコードの `ROUTE` は
  ダイヤ(xlsx由来のJSON)読み込みに。早発は現状の青(情報)扱いから**赤(最重要警告)へ反転**。
- **bus_stop_recorder_v3.html**: ほぼ流用。所要時間測定は不要。近接重複の確認カードに
  「反対車線の別ポールなら〔別の新しいバス停〕」の明示と、方向(side)タグ入力を追加検討。
- **データフロー**: ダイヤ表(画像) → `bus_diagram.xlsx` → 便JSON。位置は記録ツール(順走) → マスタ。
  モニターが (route,seq) で結合して早発監視。

---

## 7. 未確定・今後の検討

- ★印(和光入口/和光園前/中心商店街)の意味 → 運転手に要確認。
- R(近接判定距離)の最適値、GPS精度が悪い場面での誤判定対策(精度が閾値超なら判定保留 等)。
- 対象停をGPSで自動追従するか、運転手の手動送りボタンも用意するか。
- モニターへのダイヤ取り込み形式(xlsx→JSON変換の置き場所)。
- 業務記録アプリ/貸切バス/共有DBは handoff の方針を維持(本設計の範囲外)。
