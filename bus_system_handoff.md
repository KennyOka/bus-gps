# バス運行支援システム 検討まとめ(Claude Code引き継ぎ用)

作成日: 2026-07-26
目的: Claude.aiでの検討内容を、Claude Codeでの実装セッションに引き継ぐためのドキュメント。

---

## 1. プロジェクト概要

路線バスの運行をGPSで支援するシステム。将来的には、別途進行中の「Bus Operation History Management App(運転手の業務記録アプリ)」とデータベースを共有する構想がある。

対象業務は3種類:
1. **路線バス** — 決まった路線・ダイヤがある(バス停・定刻あり)
2. **回送・休憩** — 業務記録アプリ側の概念。バス停以外の地点(車庫・休憩場所)への移動
3. **貸切バス**(将来機能) — 決まった路線がなく、当日/前日に地名や住所で行程が指定される。時刻の概念はなく、走行経路の実績記録が目的

---

## 2. これまで作成したプロトタイプ

### 2-1. 運行モニター(ダッシュボード) — `bus_tracker.html`

運転手向け。GPSで現在地を取得し、ダイヤと比較して「今何分早い/遅れているか」と「次のバス停」を表示する。

**アルゴリズム:**
- 各バス停の緯度経度を、始点を基準にした平面座標(x, y メートル)に変換(等長円筒図法の簡易近似)
- 現在地から、隣接するバス停を結ぶ各区間(線分)への垂線距離を計算し、最も近い区間とその区間内の位置(0〜1の内分点 `t`)を特定
- その区間の定刻(始発からの分数)を `t` で線形補間し「本来の到達時刻(分)」を算出
- 実際の経過時間(運行開始からの分数) − 本来の到達時刻 = 遅延(分)。正なら遅れ、負なら早着
- 次のバス停 = 現在の区間の先にあるバス停

**モード:**
- 実GPSモード(`navigator.geolocation.watchPosition`)
- デモ走行モード(スライダーで路線上の位置を疑似的に動かし、遅延オフセットも手動で試せる。実機なしでの動作確認用)

**UI設計方針:** 運転中の一瞥性を優先。ダッシュボード風の暗色背景、遅延は大きな数字+信号色(緑=定刻、黄=軽微な遅れ、赤=大幅な遅れ)、下部に路線全体の進行バー。

---

### 2-2. バス停記録ツール — `bus_stop_recorder_v3.html`

会社からダイヤ情報(バス停の緯度経度)がまだ提供されないため、実際に車で走ってバス停ごとにGPSを記録するためのツール。将来的にはこのデータが上記モニターの入力になる。

**2つのモード:**
- **順走記録モード**: ダイヤ通りに順番に走りながら記録。記録順序と、起点からの経過時間(暫定の定刻候補)を保持する
- **単独登録モード**: 順序に関係なく、偶然見つけたバス停をその場で単発登録。後から「→ 順路に追加」で順走リストに組み込み可能

**近接重複チェック:**
- 新規記録時、既存の登録地点から50m以内に近い地点がないか判定(ハーバサイン距離)
- ヒットした場合は確認カードを表示し、「同じバス停として記録(既存IDを再利用)」か「別の新しいバス停として登録」かを選ばせる
- 50mという閾値は暫定値。実走してみて広すぎ/狭すぎであれば調整の余地あり

**マスタ読み込み機能(複数路線・重複バス停への対応):**
- 路線が20以上あり、1つのバス停を複数路線が共有するため、セッションをまたいだ重複チェックが必要
- 記録開始前に、過去のセッションで書き出した `bus_stops_master.json` を読み込むことで、そのバス停群を重複チェックの母集団に加えられる
- マスタから読み込んだだけで今回未使用の地点は一覧表示せず(件数が多くなるため)、裏側で照合にのみ使う
- 書き出すJSONには、今回新規に見つかった地点 + 読み込んだマスタ全体を丸ごと含める → このファイルが路線を追うごとに育っていく累積マスタになる

**内部データ構造(このプロトタイプの状態):**
```
locations: [{ id, name, lat, lng, accuracy, capturedAt, fromMaster }]   // 重複なしのバス停マスタ
sequence:  [{ locationId, capturedAt }]                                  // 順走記録の並び
```

**書き出すJSON形式:**
```json
{
  "name": "路線名",
  "stops": [
    { "locationId": 1, "name": "名瀬バスセンター", "lat": ..., "lng": ..., "scheduledMin": 0 }
  ],
  "locations": [
    { "locationId": 1, "name": "名瀬バスセンター", "lat": ..., "lng": ..., "accuracy": 8 }
  ]
}
```

---

## 3. データベース設計方針(路線バス ⇔ 業務記録アプリ 共有を見据えて)

### 3-1. 共有マスタ(2アプリ共通にする想定)

| テーブル | 主なカラム | 役割 |
|---|---|---|
| `locations` | `location_id`, `name`, `lat`, `lng`, `location_type`(`stop`/`depot`/`rest_point`/`waypoint`) | バス停・車庫・休憩場所などあらゆる「地点」を一元管理。現状のプロトタイプの`locations`はこの前身で、将来 `location_type` 列を足して一般化する想定(**現段階ではプロトタイプへの変更は不要、方針として認識しておくのみ**) |
| `routes` | `route_id`, `route_name` | 路線という概念自体の識別子(ダイヤ改正があっても同じID) |
| `schedules` | `schedule_id`, `route_id`, `effective_date`, `label` | ダイヤ改正のバージョン管理 |
| `schedule_stops` | `schedule_id`, `location_id`(FK), `sequence_no`, `scheduled_min` | ダイヤ明細。`location_id`は`location_type=stop`の地点を参照。1つの`location_id`を複数の`schedule_id`(=複数路線)が参照できる正規化構造 |

### 3-2. 業務記録アプリ側で追加が必要なテーブル

| テーブル | 主なカラム | 役割 |
|---|---|---|
| `drivers` | 運転手マスタ | |
| `duty_rosters` | `duty_id`, 対象日, 運転手ID, 車両 | 行路(勤務ダイヤ) |
| `duty_segments` | `duty_id`, `sequence_no`, `segment_type`(`passenger`/`deadhead`/`break`/`standby`) | 行路の時系列明細。`passenger`は`schedule_id`参照、`deadhead`は`from_location_id`/`to_location_id`で`locations`を直接参照、`break`/`standby`は`location_id`+開始終了予定時刻 |
| `operation_records` | 予定(`duty_segments`)に対する実績 | GPSアプリで取得した実位置・実時刻を紐づけ、予定と実績の差分を記録 |

**設計思想:** 回送(`deadhead`)を無理に旅客ダイヤ(`schedule_stops`)に押し込まず別の実体として持たせることで、GPSモニターの遅延計算ロジック(`schedule_stops`だけ見ればよい)を、業務記録アプリ側の複雑さ(回送・休憩)から独立させている。

### 3-3. 貸切バス(将来機能、路線がない業務)

貸切バスは「固定路線+ダイヤ」ではなく「その日限りの行程+経路の実績記録」という性質が異なるデータ。

| テーブル | 主なカラム | 役割 |
|---|---|---|
| `charter_trips` | `trip_id`, 日付, 運転手, 車両, 備考 | `routes`/`schedules`に相当するが使い捨て(改正の概念なし) |
| `charter_trip_waypoints` | `trip_id`, `sequence_no`, `location_id`, メモ | 目的地リスト。**時刻は持たせない**。順番のみが意味を持つ |
| (実績ログ) | `(trip_id, lat, lng, timestamp)` の連続ログ、または目的地到着ごとに1点記録 | 「定刻との比較」ではなく走行経路そのものの記録。`schedule_stops`とは別物として扱う |

**貸切の目的地登録経路について:** 観光地・住所は現地でGPSボタンを押すのではなく、地名・住所入力→ジオコーディングで`locations`に追加される想定。つまり`locations`は将来「現地記録」と「地名/住所入力」という2つの登録経路を持つことになる(将来的に`source`列などで区別する余地を残しておく)。

---

## 4. 未解決・要検討事項

- 近接重複チェックの閾値(現在50m固定)を、路線・道路事情に応じて調整可能にすべきか
- `locations`テーブルへの`location_type`・`source`列の追加タイミング(業務記録アプリ側の設計が固まった時点で統合予定)
- 貸切バスのジオコーディング方式(Google Maps Geocoding APIなど、外部API選定は未検討)
- 貸切バスの実績記録の粒度(一定間隔の連続ログ vs 目的地到着ごとの単発記録)

---

## 5. 動作環境に関する注意(iPhone実機テスト時)

- GPS(Geolocation API)は **HTTPS配信のページ**でのみ動作する。`file://`で直接開いたり、Claude.aiのプレビュー(iframe)内では `Origin does not have permission` エラーになる
- 実機テスト時は GitHub Pages 等でHTTPS配信し、SafariでそのURLを開く必要がある
- iOS設定側でも「設定 → プライバシーとセキュリティ → 位置情報サービス」でSafari(または対象サイト)の許可が必要

---

## 6. これまでに作成したファイル(参考)

- `bus_tracker.html` — 運行モニター(ダッシュボード)プロトタイプ
- `bus_stop_recorder.html` — バス停記録ツール 初版
- `bus_stop_recorder_v2.html` — 順走/単独モード + 近接重複チェック追加版
- `bus_stop_recorder_v3.html` — マスタ読み込み機能追加版(最新)
